# AGENTS.md

このファイルは、`ptaorg/ptaorg.github.io` 全体の編集に適用します。

## サイトの性格

ptaorg.com は、PTAの任意加入、学校とPTAの公私分離、個人情報保護、会費徴収、教職員関与、学校施設利用などを、本文の論理で読ませる資料サイト・論考サイト・行政向け説明サイトです。

カード、タグ、リンク一覧を増やして本文の代わりにしません。カード型ポータル化、ハブページの乱造、見た目だけの整理を目的にした本文削減は行いません。見出し、罫線、左線、余白、強調枠、資料種別ラベルなどは、読み物としての論理構造を補助するために使います。

## 編集前に必ず確認すること

編集前に、次の3点を明示します。

1. 対象ファイル
2. 変更方針
3. 変更しない範囲

大規模改修から始めず、必要最小限のファイルだけを変更します。GitHub Webアップロードの100件制限も踏まえ、不要な生成物や一時ファイルを増やしません。

## 本文と根拠の保全

- 既存の文章を、明示的な依頼なしに勝手に短くしません。
- 本文の論理説明をカード、タグ、リンク集だけに置き換えません。
- 重要な説明、法的整理、行政回答の解説は、原則として静的HTML本文を正とします。
- 法令、通知、判例、行政資料、URLは、確認できていないものを断定しません。
- 固有の一次資料、引用元、アンカー、既存URLを不用意に削除・変更しません。
- 生成ファイルを直接修正する前に、生成元スクリプトとの関係を確認します。
- 一括置換や一回限りの変換スクリプトは、必要性と対象範囲を確認してから使います。

## 変更の進め方

小さい差分を優先します。本文変更、共通UI変更、生成物更新、キャッシュ版数更新を可能な限り分離し、原因を追える状態に保ちます。

共通ナビゲーションや生成物を更新する場合は、既存の保守スクリプトを優先し、手作業で多数ページを書き換えません。

## 必須チェック

変更後は原則として次を確認します。

```bash
npm test
git diff --check
git diff --stat
```

`npm test` は、未加工のチェックアウトに対して実行し、検査前に生成ファイルを自動更新して不整合を隠してはいけません。

表示に影響するHTML/CSS/JavaScriptを変更した場合は、PC幅とスマートフォン幅で主要ページをブラウザ確認します。ブラウザ確認を実施できなかった場合は、実施済みと書かず、その旨を明記します。

## 完了報告

完了時は、変更ファイル数、変更した内容、変更しなかった範囲、実行したチェック、ブラウザ表示確認の有無を明示します。

ZIPを作成する場合は、収録ファイル数、配置場所、変更内容、ブラウザ表示確認の有無を必ず明示します。
