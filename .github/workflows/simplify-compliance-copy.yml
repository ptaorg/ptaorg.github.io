name: Archived compliance copy simplifier

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm archived state
        run: echo 'Automatic compliance copy rewriting is disabled.'
