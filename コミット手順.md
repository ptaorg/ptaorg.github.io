# GitHubへの反映手順

私が実際に変更したのは **6ファイル** です。あなたはこれをコミット＆プッシュするだけです。

## 変更内容の要約

| ファイル | 変更 |
|---|---|
| `robots.txt` | ClaudeBot / PerplexityBot / Google-Extended / bingbot / Applebot / CCBot を明示的に許可（既存の除外設定・OpenAI設定はそのまま維持） |
| `pta-membership-optin.html` | FAQ構造化データ（Q&A 5件）を追加 |
| `fee-collection.html` | FAQ構造化データ（Q&A 3件）を追加 |
| `membership.html` | FAQ構造化データ（Q&A 3件）を追加 |
| `personnel.html` | FAQ構造化データ（Q&A 2件）を追加 |
| `facilities.html` | FAQ構造化データ（Q&A 2件）を追加 |

すべて**追加のみ**。既存のコンテンツ・デザイン・他の構造化データには一切手を付けていません。JSON-LDの構文チェックも通過済みです。

---

## 反映方法（どちらか選ぶ）

### 方法A：ブラウザでアップロード（かんたん・おすすめ）

1. `変更ファイル` フォルダの中の6ファイルをダウンロード
2. GitHubで `ptaorg/ptaorg.github.io` リポジトリを開く
3. 「Add file」→「Upload files」
4. 6ファイルをドラッグ＆ドロップ（同名ファイルは自動で上書きされます）
5. 下の「Commit changes」を押す

→ 1〜2分でサイトに反映されます。

### 方法B：git コマンド（ローカルにリポジトリがある場合）

変更した6ファイルを上書きして、以下を実行：

```bash
git add robots.txt pta-membership-optin.html fee-collection.html membership.html personnel.html facilities.html
git commit -m "AI最適化: robots.txtにAIクローラー明示 + 主要5ページにFAQ構造化データ追加"
git push
```

---

## 反映後の確認（任意）

- `https://ptaorg.com/robots.txt` を開いて、ClaudeBot等が入っているか確認
- Googleの「リッチリザルトテスト」に `https://ptaorg.com/pta-membership-optin.html` を入れ、「よくある質問」が検出されればOK
  - https://search.google.com/test/rich-results

---

## このあと効果を伸ばすには（コード作業ではない部分）

サイトの技術的な作りは、正直もう完成度が高いです（199ページに構造化データ、llms.txt、sitemap 198件、すべて実装済み）。
ここから先の伸びしろは**サイトの外**にあります：

1. **Google Search Console / Bing Webmaster Tools にサイトマップを登録**（まだなら）
   - Bingは特に重要（ChatGPT・Copilotの検索土台）
2. **他サイトからの被リンク・言及を増やす**
   - note・YouTube・X から各解説ページへのリンク
   - 報道・自治体資料・PTA関連サイトからの言及
3. 「PTA 会費 引き落とし 拒否」など、悩む人が実際に打つ言葉を見出しにしたページを増やす

これらは「AIが拾う土台（検索順位）」を上げる作業で、コンテンツ改修より効果が大きい段階に来ています。
